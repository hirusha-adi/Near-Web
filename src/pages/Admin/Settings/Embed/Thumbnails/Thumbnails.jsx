import { useEffect } from "react";

const Thumbnails = () => {
	useEffect(() => {
		document.title = `Embed Thumbnails | Near`;
	});

	return (
		<>
			Thumbnails
		</>
	);
};

export { Thumbnails };
